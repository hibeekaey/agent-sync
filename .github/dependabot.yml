version: 2
updates:
  # Actions are pinned to commit SHAs; Dependabot keeps the pins current and
  # rewrites the trailing version comment, so pinning costs no readability.
  - package-ecosystem: github-actions
    directory: /
    schedule:
      interval: weekly
    commit-message:
      prefix: ci
